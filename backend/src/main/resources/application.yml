# kangy backend configuration
server:
  port: 8080

spring:
  application:
    name: kangy-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui

